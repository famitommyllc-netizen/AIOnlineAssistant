# Change Log Save
- Saved At: 2026-03-08 18:51:12 JST
- Scope: 2026-03-08 時点の追加実装・不具合修正まとめ
- Format: core.txt 準拠（日時見出し + カテゴリ別 + 連番）

## 2026-03-08 追加・更新内容

### Core / Runtime
- [1] `/api/state` の肥大化対策としてメッセージ/通知返却数の上限運用を継続。
- [2] 日次アーカイブ機能を導入し、前日以前の `messages/reminders` を `logs/history/YYYY-MM-DD.jsonl` へ退避。
- [3] 履歴参照APIを追加（`/api/history/dates`, `/api/history/:YYYY-MM-DD`）。
- [4] サーバー起動ログに LAN URL を表示する構成を追加（`LAN_HOST` 環境変数）。

### Chat / Menu UX
- [5] 通常案内文を秘書文言へ更新し、案内文下にアプリボタンを表示する挙動へ変更。
- [6] アプリボタンの表示順・表示対象を `settings.appMenu` で管理する構成を追加。
- [7] アプリボタン表示レイアウトを設定化（`grid3` / `single`）。
- [8] アプリボタンの見た目を iPhone風アイコン主体に調整。
- [9] アプリボタンのアイコンサイズを設定可能化（`settings.appMenu.iconSize`）。
- [10] デフォルトアイコンサイズを 68px から 102px（1.5倍）へ更新。

### Mode / UI Behavior
- [11] 通常モード時は `モード終了` エリアを非表示に変更。
- [12] 通常モードでの表示を簡潔化し、必要時のみモード関連UIを表示する構成へ調整。

### Task / Cache / Reliability
- [13] タスク完了・再開の反映遅延対策として、`tasks.js` 側の取得を `cache: no-store` 化。
- [14] Service Worker で `/api/*` をキャッシュ除外し、状態更新反映の遅れを抑止。

### Number Tool
- [15] section単位の並び替え（`sectionOrder`）に加え、form内 row 単位の並び替えを追加。
- [16] row移動は `位置変更モード` 中の `↑/↓` 操作で実施し、保存後に再読込反映。
- [17] row順序の保存キーを `calculators.numberMemo.ui.formRowOrder` として運用開始。

### Profit Tool
- [18] section単位並び替えを維持しつつ、form内 row 単位並び替えを追加。
- [19] `利益計算の設定` / `位置変更モード` を `controls` section 化し、section移動対象へ追加。
- [20] `controls` section のデフォルト位置は最下段。
- [21] row順序の保存キーを `calculators.profit.ui.formRowOrder` として運用開始。
- [22] mobile圧縮レイアウトを一部導入後、デバッグ優先でスクロール不能問題を解消（overflow制約を解除）。

### Smartphone / Access
- [23] 同一LANからのアクセス手順を明確化（`http://<Mac_IP>:3000`）。
- [24] `localhost` 共有不可問題に対する起動時案内を整備。

### Logs / Debug
- [25] runログ確認フローを継続し、`chat_received` 到達可否・API応答可否で切り分ける運用を継続。
- [26] 重大な backend error は直近 run で未検出（フロント/キャッシュ起因を優先確認する運用）。

## 反映先（主な更新ファイル）
- `server.js`
- `public/app.js`
- `public/sw.js`
- `public/settings.html`
- `public/settings.js`
- `public/styles.css`
- `public/number-tool.html`
- `public/number-tool.js`
- `public/profit-tool.html`
- `public/profit-tool.js`

## 備考
- 本記録は「ここまでやったこと」の保存用スナップショット。
- 既存仕様書 `docs/spec_master_20260308_092116.md` はマスター仕様として継続利用。
